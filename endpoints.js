const rootURL = 'http://10.0.2.2:3000';

export const login = `${rootURL}/login`;

export const newOrder = `${rootURL}/order`;

export const allOrders = `${rootURL}/orders`;

export const depth = `${rootURL}/depth`;

export const allTrades = `${rootURL}/tradehistory`;
